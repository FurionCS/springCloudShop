server:
  port: 8886
eureka:
  instance:
    hostname: eureka2
  client:
    service-url:
      defaultZone: 'http://eureka1:8888/eureka/'
    fetch-registry: true
    register-with-eureka: true
spring:
  application:
    name: 'eureka-server'